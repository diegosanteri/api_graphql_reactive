type Book {
    id: String!
    title: String!
    isbn: String!
    author: Author
}

extend type Query {
    findAllBooks: [Book]!
    countBooks: Long!
}

extend type Mutation {
    newBook(title: String!, isbn: String!, author: String!) : Book!
    deleteBook(id: String!) : Boolean
}